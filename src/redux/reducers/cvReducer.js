//import {  } from "types";

const initialState = {};

export const cvReducer = (state = initialState, action) => {
	switch (action.type) {
		default:
			return state;
	}
};

const initialStateProject = {};

export const cvProjectReducer = (state = initialState, action) => {
	switch (action.type) {
		default:
			return state;
	}
};

const initialStateStudy = {};

export const cvStudyReducer = (state = initialState, action) => {
	switch (action.type) {
		default:
			return state;
	}
};

const initialStateSkill = {};

export const cvSkillReducer = (state = initialState, action) => {
	switch (action.type) {
		default:
			return state;
	}
};

const initialStateLanguage = {};

export const cvLanguageReducer = (state = initialState, action) => {
	switch (action.type) {
		default:
			return state;
	}
};

const initialStateExperience = {};

export const cvExperienceReducer = (state = initialState, action) => {
	switch (action.type) {
		default:
			return state;
	}
};

const initialStateCertificate = {};

export const cvCertificateReducer = (state = initialState, action) => {
	switch (action.type) {
		default:
			return state;
	}
};
